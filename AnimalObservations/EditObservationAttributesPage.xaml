<MobileClient:MobileApplicationPage
    x:Class="AnimalObservations.EditObservationAttributesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:MobileClient="clr-namespace:ESRI.ArcGIS.Mobile.Client;assembly=ESRI.ArcGIS.Mobile.Client"
    xmlns:local="clr-namespace:AnimalObservations"
    xmlns:toolkit="http://schemas.microsoft.com/wpf/2008/toolkit"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    >
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.Resources>
                <Style TargetType="Label">
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Padding" Value="10,5"/>
                </Style>
                <Style TargetType="TextBox">
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Margin" Value="10"/>
                </Style>
                <Style TargetType="ComboBox">
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Margin" Value="10"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Column="0" Grid.Row="0" Target="{Binding ElementName=angleTextBox}" >_Angle:</Label>
            <Label Grid.Column="0" Grid.Row="1" Target="{Binding ElementName=distanceTextBox}" >_Distance:</Label>
            <TextBox Grid.Column="1" Grid.Row="0" x:Name="angleTextBox" Text="{Binding Task.ActiveObservation.Angle}"/>
            <TextBox Grid.Column="1" Grid.Row="1" Name="distanceTextBox" Text="{Binding Task.ActiveObservation.Distance}"/>
        </Grid>
        <StackPanel HorizontalAlignment="Center" DockPanel.Dock="Bottom" Orientation="Horizontal">
            <Button Width="28" Click="backButton_Click" FontSize="16" FontWeight="Bold" Padding="3,0,3,3">b</Button>
            <Label Margin="5" Content="{Binding Task.OpenObservations.Count}"/>
            <Button Width="28" Click="forwardButton_Click" FontSize="16" FontWeight="Bold" Padding="3,0,3,3">f</Button>
        </StackPanel>

        <toolkit:DataGrid Name="gridView" ItemsSource="{Binding Task.ActiveObservation.BirdGroups}" IsTabStop="True" />

<!--       <StackPanel Orientation="Horizontal">
            <Button Width="28" Click="AddButton_Click" FontSize="16" FontWeight="Bold" Padding="3,0,3,3">+</Button>
            <Button Width="28" Click="RemoveButton_Click" FontSize="16" FontWeight="Bold" Padding="3,0,3,3">-</Button>
        </StackPanel>
         -->
        <!--  
        <ListView Margin="10"  ItemsSource="{Binding Task.ActiveObservation.BirdGroups}" Name="birdsListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Group Size" DisplayMemberBinding="{Binding GroupSize}"/>
                    <GridViewColumn Header="Behavior" DisplayMemberBinding="{Binding Behavior}"/>
                    <GridViewColumn Header="Species" DisplayMemberBinding="{Binding Species}"/>
                    <GridViewColumn Header="Comments" DisplayMemberBinding="{Binding Comments}"/>
                </GridView>
            </ListView.View>
        </ListView>
            <ListBox Name="attributeListBox" Margin="10" Background="Snow"
                 ItemsSource="{Binding EditableAttributes}"
                 HorizontalContentAlignment="Stretch"
                 Grid.IsSharedSizeScope="True"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 KeyboardNavigation.TabNavigation="Continue">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="IsTabStop" Value="False" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="5" Margin="10" BorderBrush="DarkBlue" BorderThickness="2" Background="AliceBlue" CornerRadius="5" KeyboardNavigation.IsTabStop="False">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="5" Color="DarkBlue" Opacity=".4" RenderingBias="Performance" />
                        </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition SharedSizeGroup="title" Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                        
                            <TextBlock Grid.Row="0" Grid.Column="0" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="10" Text="{Binding Path=EditCaption}"/>
                            <StackPanel Grid.Row="0" Grid.Column="1">
                                <TextBox  Margin="5" HorizontalAlignment="Stretch" Text="{Binding Path=Value}"/>
                                <TextBlock  Text="{Binding Path=DisplayString}" />
                                <TextBlock Foreground="Red" FontStyle="Italic" Text="{Binding Path=ErrorMessage}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
            -->
    </DockPanel>
</MobileClient:MobileApplicationPage>

  
